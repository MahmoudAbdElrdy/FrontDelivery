/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CustomDatePipeDirective } from './CustomDatePipe.directive';

describe('Directive: CustomDatePipe', () => {
  it('should create an instance', () => {
    const directive = new CustomDatePipeDirective();
    expect(directive).toBeTruthy();
  });
});
